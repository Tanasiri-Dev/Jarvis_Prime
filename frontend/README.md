# Jarvis Prime Frontend

React + TypeScript + Vite frontend for Jarvis Prime.

React owns UI orchestration. Heavy work belongs in Web Workers. Canvas rendering belongs in workers through OffscreenCanvas.

## Local Dev

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173.

## Verification

```bash
npm run typecheck
npm run build
```
